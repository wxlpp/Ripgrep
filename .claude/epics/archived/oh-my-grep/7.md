---
name: Result correctness: contiguous context, separators, CRLF, binary detection, -A/-B 0
status: closed
created: 2026-09-14T07:12:45Z
updated: 2026-09-14T07:52:13Z
github: https://github.com/wxlpp/oh-my-grep/issues/7
depends_on: [6]
parallel: false
conflicts_with: [8, 9]
---

# Task: Result correctness: contiguous context, separators, CRLF, binary detection, -A/-B 0

## Description
Fix context attribution and related line-level correctness.

## Acceptance Criteria
- [ ] Fixture `a b c d HIT e HIT f`: -B 2 → L5 [c,d], L7 [e]; -C 1 → L5 before [d] after [e], L7 before [] after [f]; -A 3 → L5 after [e], L7 after [f]
- [ ] Pending after-context reduced to a single slot; v0.3 eviction/window code and its now-meaningless tests removed or rewritten
- [ ] `formattedAsText` emits `--` between non-contiguous groups when any context is present; output equals `rg --no-heading -n -C1` / `-B2` on the fixture (checked in test with expected strings)
- [ ] CRLF: no trailing \r on match/context lines (PR #4 content, comments trimmed); PR #4 closed as superseded
- [ ] Binary: files containing NUL skipped by default; `Options.searchBinary` / `-a, --text` searches them
- [ ] `-A 0` / `-B 0` override `-C` (RipgrepArgs Int?)
- [ ] Rust + Swift tests; macOS swift test green

## Technical Details
sink.rs: clear before buffer on match; on new match flush pending; context_break flush. search.rs: `SearcherBuilder::binary_detection`. Parse.swift merge rules.

## Dependencies
001.

## Effort Estimate
- Size: M
- Hours: 5

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
