---
name: Ignore rules, required paths, workingDirectory, warnings, symlink loop
status: open
created: 2026-09-14T07:12:45Z
updated: 2026-09-14T07:16:37Z
github: https://github.com/wxlpp/oh-my-grep/issues/8
depends_on: [7]
parallel: false
conflicts_with: [7, 9]
---

# Task: Ignore rules, required paths, workingDirectory, warnings, symlink loop

## Description
Make ignore semantics and path/error handling explicit and platform-independent.

## Acceptance Criteria
- [ ] `Options.requireGit` (default true) → `WalkBuilder::require_git`; `--no-require-git` flag
- [ ] Tests build their fixture tree in a temp dir outside any git repo and assert both requireGit true/false; `.ignore` file respected regardless; tests pass on macOS and iOS simulator
- [ ] `search(in: [])` throws invalidArguments (Rust + Swift)
- [ ] `OhMyGrep.run(_:workingDirectory:)`, `handleToolCall(_:workingDirectory:)`: relative and default paths resolved against it; relative path with no workingDirectory → invalidArguments
- [ ] `SearchResult.warnings: [Warning]` from walk errors and search_path errors, capped at 100 with a final overflow note
- [ ] Self-referencing symlink fixture (created at test time) terminates and returns no duplicates
- [ ] Tests green

## Technical Details
Walk errors come as `Err(ignore::Error)` in the walker closure; searcher errors from `search_path` return value. Collect via a Mutex<Vec> or channel with a counter.

## Dependencies
002.

## Effort Estimate
- Size: M
- Hours: 5

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
